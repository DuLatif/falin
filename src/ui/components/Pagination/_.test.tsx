/* eslint-disable @typescript-eslint/no-empty-function */
import { describe, test } from "vitest"

describe("Pagination", () => {
  test("should render properly", () => {})

  test("should contains id attribute", () => {})

  test("should disabled prev button", () => {})
})
